# Google Analytics 4 — only loads after user clicks "Accept all cookies"
# Create in Netlify: Site settings → Environment variables
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Homepage “coming soon” mode — set to "true" to show the teaser home instead of the full site
# NEXT_PUBLIC_COMING_SOON=true
